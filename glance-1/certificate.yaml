apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: glance-1-tls-cert
  namespace: glance-1
spec:
  secretName: glance-1-ts-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "glance.pimonlab.xyz"
  dnsNames:
  - "glance.pimonlab.xyz"
