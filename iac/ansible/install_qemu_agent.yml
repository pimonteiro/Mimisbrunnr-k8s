---
- name: Install QEMU Guest Agent on K3s Nodes
  hosts: workers,masters
  become: yes
  tasks:
    - name: Install qemu-guest-agent
      apt:
        name: qemu-guest-agent
        state: present
        update_cache: yes

    # - name: Ensure agent is started and enabled
    #   systemctl:
    #     name: qemu-guest-agent
    #     state: started
    #     enabled: yes

    # - name: Verify agent is running
    #   command: systemctl status qemu-guest-agent
    #   register: agent_status
    #   changed_when: false

    # - name: Show status
    #   debug:
    #     msg: "Agent is {{ 'running' if 'active (running)' in agent_status.stdout else 'FAILED' }}"
