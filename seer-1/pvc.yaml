apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seer-1-data-pvc
  namespace: seer-1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-nfs
  resources:
    requests:
      storage: 5Gi
---